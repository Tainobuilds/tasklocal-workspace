/**
 * Stand-in for authentication.
 *
 * There is no sign-in yet, so the app acts as one fixed customer. Bookings the
 * flow creates are attributed here, and the bookings page defaults to it.
 * Replace both usages with the real session when auth arrives.
 */
export const DEMO_CUSTOMER_ID = 'cust_00042';
